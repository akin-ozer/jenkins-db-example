name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: apt update
      run: sudo -i && apt-get update && exit
    
    - name: apt install kubectl
      run: sudo -i && apt-get install -y kubectl && exit
    
    - name: k version
      run: kubectl version
